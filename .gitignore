# ── 자격 증명 (절대 커밋 금지) ─────────────────────────
.env
.env.local
.env.*.local
*.pem
*.key
credentials.json

# ── 교과 팩 빌드 산출물 ────────────────────────────────
# 성취수준 원문·예시문항은 교육부/평가원/시도교육청 저작물이다.
# 각자 로컬 원자료로 빌드한다:  python scripts/build_refs.py
# 공공누리 제1유형 확인 시 이 줄을 지우고 출처 명시 후 함께 배포 가능.
subject/data/

# ── 빌드 중간 산출물 ──────────────────────────────────
.work/

# ── 외부 포털에서 내려받은 원자료 ──────────────────────
downloads/

# ── 원본 자료 (로컬 참고용, 배포 대상 아님) ─────────────
*.pdf
*.hwp
*.hwpx
!docs/**/*.pdf

# ── 도구 세션 상태 (개인 작업 이력) ──────────────────
.omc/
.claude/

# ── 런타임 산출물 ──────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
node_modules/
dist/
build/
*.log

# ── OS / 편집기 ───────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
